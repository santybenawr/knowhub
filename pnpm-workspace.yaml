packages:
  - '.'

# Dependencies allowed to run install scripts. pnpm blocks them by default;
# these four need theirs (native binaries and WASM assets), and nothing else
# in the tree does.
allowBuilds:
  esbuild: true
  unrs-resolver: true
  '@tailwindcss/oxide': true
  sharp: true

onlyBuiltDependencies:
  - esbuild
  - unrs-resolver
  - '@tailwindcss/oxide'
  - sharp
